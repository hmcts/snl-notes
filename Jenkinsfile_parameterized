#!groovy

@Library("Infrastructure")

def type = "java"
def product = "snl"
def component = "notes"

withPipeline("java", product, component) {
    enableDbMigration()
    enableDockerBuild()
    enableDeployToAKS()
}
