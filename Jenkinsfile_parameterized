#!groovy

@Library("Infrastructure")

def type = "java"
def product = "snl"
def component = "notes"

withPipeline(type, product, component) {
    enableDbMigration()
    enableDockerBuild()
    enableDeployToAKS()
}
